node_modules
.next
data
.env
.env.local
*.tsbuildinfo
# NOTE: test-fixtures/ is intentionally NOT ignored — the vendored fixture JSONs
# must ship with the repo so Vercel deploys include them (demo tour + Test mode).
/test-fixtures-real/
