import DemoTour from "./demo-tour";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Guided Demo — Tweet Voice Cloner",
};

export default function DemoPage() {
  return <DemoTour />;
}